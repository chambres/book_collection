# Localhost-only preview of the production-mode container.
# Cloud production still uses HTTPS and its normal Rails SSL configuration.
server {
  listen 80;
  location / {
    proxy_pass http://production:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
  }
}
